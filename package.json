{
  "name": "open-cv",
  "version": "1.0.0",
  "description": "Open-source HTML/PDF resume powered by cv.json",
  "type": "module",
  "scripts": {
    "build": "node generate.js",
    "export": "node generate.js --pdf",
    "preview": "node generate.js && python3 -m http.server 5500"
  },
  "devDependencies": {
    "puppeteer": "^24.37.5"
  }
}
